import 'package:flutter/material.dart';

// Ce widget est vide car les boutons OAuth sont supprimés
// Tu peux le supprimer complètement si tu veux, mais pour éviter les erreurs d'import,
// on garde un widget vide.
class OAuthButtons extends StatelessWidget {
  const OAuthButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
