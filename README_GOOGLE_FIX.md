This project has been rebuilt with Android support and Google Sign-In fixes.

IMPORTANT:
You MUST create BOTH:
- OAuth Android
- OAuth Web

Then replace the Web Client ID inside:
lib/config/oauth_config.dart